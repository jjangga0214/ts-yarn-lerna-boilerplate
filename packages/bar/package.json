{
  "name": "@jjangga0214/bar",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "files": [
    "dist"
  ],
  "_moduleAliases": {
    "~bar": "dist"
  },
  "scripts": {
    "postinstall": "shx mkdir -p dist",
    "build": "tsc -p tsconfig.build.json && link-module-alias",
    "clean": "shx rm -rf dist coverage *.log junit.xml ../../.build-cache/bar.tsbuildinfo && jest --clearCache",
    "test": "jest",
    "test:unit": "jest test/unit",
    "test:watch": "jest --watch",
    "test:ci": "jest --coverage --ci --reporters='jest-junit'",
    "coverage": "jest --coverage",
    "coverage:show": "live-server coverage",
    "dev": "ts-node-dev -r tsconfig-paths/register src/index.ts"
  },
  "dependencies": {
    "link-module-alias": "^1.2.0",
    "@jjangga0214/foo": "^1.0.0"
  }
}
